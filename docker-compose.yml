services:
  itsysync:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - data:/data
    environment:
      - PORT=${PORT:-8080}
      - ADMIN_TOKEN=${ADMIN_TOKEN}
    restart: unless-stopped

volumes:
  data:
